# .terraform.lock.hcl is deliberately NOT ignored: it pins the provider
# versions so a later apply cannot silently pick up a different one.
.terraform/
terraform.tfstate
terraform.tfstate.*
*.tfvars
!example.tfvars
crash.log
